'use strict';

/**
 * feature-vend-lead service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::feature-vend-lead.feature-vend-lead');
